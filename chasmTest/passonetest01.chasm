.org $0000          ;
.byte $00, $100      ; skip the zero-page; this should fill the first 256 bytes with zeros

        cli
loop:   lda #$ea
        ldx #255
        sta $00, x
        cpx #0
        BnE loop
        BRK
